I built llm-spy because I kept wanting a simple “what did my AI app just send?” button.

It runs as a local proxy and shows LLM calls in your terminal + local dashboard. No SDK hooks, no account, no telemetry.

The fun part: you can try it with a fake OpenAI-compatible provider, no API key needed.

Feedback very welcome.
