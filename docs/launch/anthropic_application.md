# Anthropic Application

llm-spy helps developers build more understandable Claude-powered applications by capturing local request/response history, parsing Messages API calls, surfacing tool-use fields, and warning about PII and prompt injection patterns.

The project is local-first and keeps audit trails private. Anthropic credits would support parser compatibility, documentation, and responsible AI examples.
