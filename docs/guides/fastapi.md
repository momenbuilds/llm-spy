# FastAPI Guide

```bash
llm-spy start
HTTPS_PROXY=http://localhost:8080 HTTP_PROXY=http://localhost:8080 uvicorn app:app --reload
```

Open your app and trigger an LLM request. Then inspect `llm-spy history` or the dashboard.
